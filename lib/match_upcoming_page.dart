import 'package:flutter/material.dart';

class FantasyPage extends StatelessWidget {
  static const routeName = "/FantasyPage";
  const FantasyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [],
      ),
    );
  }
}
